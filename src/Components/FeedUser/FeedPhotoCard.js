import FeedPhotoCard from './FeedPhotoCard.hbs';
import {BaseComponent} from '../Base/Base';

/**
 * Photo card in feed component
 */
export class FeedCardComponent extends BaseComponent {
    /**
     * Create feed card
     * @param {Array}styles
     */
    constructor({styles}) {
        super({styles});
        this.components.photo = new
    }
}
